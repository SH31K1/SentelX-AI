# Requires Python 3.10+ (sentence-transformers 5.x and torch 2.x dropped support for older versions)
 
# Pinned tightly: ChromaDB migrations between versions are irreversible.
# Do not bump this without testing against a throwaway copy of totheroot_db/ first.
chromadb>=1.5.0,<1.6.0
 
sentence-transformers>=5.0.0,<6.0.0
torch>=2.0.0,<3.0.0
numpy>=1.25.0,<3.0.0
 